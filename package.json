{
  "name": "dark-theme",
  "version": "1.0.0",
  "description": "Have you seen the different themes of the editors? There's light theme, and there's dark theme. We will be building today a toggle theme functionality where we will click a button to change the theme from dark to light.",
  "main": "index.js",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "supertest": "^6.1.3"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "scripts": {
    "test": "jest --env=jsdom",
    "start": "nodemon server.js"
  },
  "jest": {
    "verbose": true,
    "testEnvironment": "jsdom"
  },
  "author": "",
  "license": "ISC"
}
